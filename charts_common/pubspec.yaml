name: charts_common
version: 0.12.0
description: A common library for charting packages.
homepage: https://github.com/google/charts

environment:
  sdk: ">=3.1.5 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  collection:
  intl: ">=0.15.2"
  logging:
  meta: ">=1.1.1"
  vector_math: ^2.0.8

dev_dependencies:
  mockito: ^5.0.0
  test:
